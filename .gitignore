# Private executable runtime state. Durable state lives in root PROGRESS.md,
# root features.yml, and optionally harness/state/<slug>/state.md.
.harness-local/

# Optional project-owned agent scratch. No workflow lifecycle is implied.
.copilot-tracking/

# Secrets / env
.env
.env.*
!.env.example
